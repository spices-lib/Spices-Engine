name: CI for SpicesEngine Solution

on:
  push:
    branches: [ "Master" ]
  pull_request:
    branches: [ "Master" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: Generate Solution
      shell: cmd
      run: call .\Startup.bat

    - name: Build Solution
      shell: cmd
      run: call .\Scripts\Build_Debug_x64.bat